version: '2.1'
jobs:
  build:
    docker:
      - image: cimg/openjdk:23.0.1
    steps:
      - checkout
      - run: java --version
      - run: ./gradlew build

workflows:
  build_and_test:
    jobs:
      - build
